<%= form_for(@blog) do |f| %>
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :preview %><br />
    <%= f.file_field :preview %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
    <%= f.hidden_field :user_id, :value => current_user.id %>

  <div class="field">
    <%= f.label :blog_category_id %><br />
    <%= f.number_field :blog_category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
